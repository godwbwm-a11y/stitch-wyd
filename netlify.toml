[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  included_files = ["knowledge_base.md"]
  node_bundler = "esbuild"

[functions."chat"]
  timeout = 30

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
